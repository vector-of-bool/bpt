.. option:: --jobs <job-count>, -j <job-count>

    Specify the maximum number of parallel tasks that |bpt| will allow to
    execute in parallel.

    By default, will use the amount of hardware parallelism available on the
    system, plus two.
