.. option:: --out <output-path>, -o <output-path>

    Specify a :term:`filepath` to the `directory` where |bpt| will write its
    build results and use as scratch space. By default, this will be the
    ``_build/`` subdirectory of the `working directory`.
