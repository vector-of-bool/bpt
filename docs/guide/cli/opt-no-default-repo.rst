.. option:: --no-default-repo, -NDR

    Disable the default package repository for dependency/package lookup.

    This can also be enforced by setting the :envvar:`BPT_NO_DEFAULT_REPO`
    environment variable to a "truthy" value.
